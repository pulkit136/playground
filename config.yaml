# playground defaults, tweak locally if needed
log_level: info
timeout_seconds: 10
retries: 3
user_agent: playground/0.2

fetch:
  max_body_mb: 5
  follow_redirects: true

cache:
  enabled: false
  ttl_minutes: 15
